# Optional — enables the LOCAL semantic RAG (embeddings + cosine search).
# The app runs fine without these: the chatbot falls back to keyword search
# plus exact SQL totals, and still uses Gemini/Ollama for the final answer.
# These are large (they pull in PyTorch), so install only if you want full
# on-device semantic retrieval.
#
#   pip install -r requirements-optional.txt
#
sentence-transformers==3.0.1
numpy==1.26.4
